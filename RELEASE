
RELEASE 1.0.9
  - use GHCR for development
  - add build date and revision to local dev container builds
  - fix nightly and dev workflows
  - add github release build after tag

Use the image with:

```
$ docker container run --rm -it rossbachp/multiarch-example:1.0.9
$ docker container run --rm -it --platform linux/arm64 rossbachp/multiarch-example:1.0.9
```

